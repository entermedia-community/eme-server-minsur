{
	"response": {
		"status": "ok",
		"userid": "$user.id"
	},
	"results": [
    #foreach($channel in $userchannels) #if( ${foreach.count} != 1 ), #end
      {
        "id": "$channel.id",
        "name": "$channel.name", 
        "date": "$channel.refreshdate"
      }
    #end
  ]
}
