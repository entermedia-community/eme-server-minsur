{
	"response": {
		"status": "ok",
		"userid": "$user.id"
	},
	"results": [
    #foreach($message in $messages) 
      #if( ${foreach.count} != 1 ), #end
      {
        "playbackentityid": #jesc($channel.playbackentityid),
        "channel": #jesc($message.channel),
        "playbackentitymoduleid": #jesc($message.playbackentitymoduleid),
        "messageid": #jesc($message.id),
        "entityid": #jesc($channel.entity),
        "message": #jesc($message.message),
        "broadcastall": #jesc($message.broadcastall),
        "command": #jesc($message.command),
        "catalogid": #jesc($message.catalogid),
        "chatterboxhome": #jesc($message.chatterboxhome),
        "functionname": #jesc($message.functionname),
        "channeltype": #jesc($message.channeltype),
        "name": #jesc($message.name),
        "moduleid": #jesc($message.moduleid),
        "user": #jesc($message.user)
      }
    #end
  ]
}
