{
	"response": {
		"status": "ok",
		"userid": "$user.id"
	},
	"results": [
    #foreach($topic in $topics)
    #if($foreach.count > 1),#end
    {
      "id":"$topic.id",
      "thumbnail":"$siteroot$mediaarchive.asLinkToPreview($topic.primarymedia, 'image200x200')",
      "name":"#text($topic)",
      "description":"$topic.longcaption"
    }
    #end
  ]
}
